import { View } from "react-native";
import { FontAwesome } from '@expo/vector-icons';

const LifePip: React.FC = () => {
    return (
        <View>
            <FontAwesome color='gold' name='dot-circle-o' size={70} />
        </View>
    );
}

export default LifePip;